events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://10.89.0.10:8000;  # schedule.py
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /news {
            proxy_pass http://10.89.0.9:5000/;  # ✅ port 5000 for app.py
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
